# 第二部分：项目实践与对照

理论讲清楚之后，才适合回到真实项目。

这一部分不把 `Claude Code`、`OpenClaw`、`Harness` 当成宣传对象，而是把它们当成三种不同的工程样本：

- `Claude Code`：本地代码代理如何做成长期交互运行时。
- `OpenClaw`：开源数字员工框架如何拆分入口、执行器和技能。
- `Harness`：为什么 Agent 的稳定性来自运行时，而不是来自模型本身。

这一部分的写法会遵守一个原则：先看主链路，再看模块；先看工程问题，再看功能表象。

只有这样，项目拆解才不会沦为“源码旅游”。
